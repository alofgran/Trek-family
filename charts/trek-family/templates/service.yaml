apiVersion: v1
kind: Service
metadata:
  name: {{ include "trek-family.fullname" . }}
  labels:
    app: {{ include "trek-family.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "trek-family.name" . }}
